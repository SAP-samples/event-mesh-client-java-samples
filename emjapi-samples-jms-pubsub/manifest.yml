---
# Default Manifest with single input for sample
applications:
- name: emjapi-samples-jms-pubsub
  memory: 768M
  path: target/emjapi-samples-jms-pubsub.jar
  random-route: false
  services:
    # This must point to an existing `event-mesh` service instance
    #- name: <event-mesh-service-instance-name>
    #  parameters:
    #    xsuaa:
    #      credential-type: x509
    #      x509:
    #        validity: 7
    #        validity-type: DAYS